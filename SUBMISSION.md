# moonphysics · 黑客松申报书

## 一、项目简介

**moonphysics —— 浏览器端 2D 物理沙盒，求解器全程由 MoonBit 编译为 WebAssembly。**
Verlet 积分 + 距离约束 + 均匀空间哈希 + 位置法碰撞响应，支撑小球、刚体箱、吊绳与可撕裂布料四类物体：随手抓取、甩飞、引爆、撕布，几百粒子稳定 240fps；内置 wasm 与同算法 JavaScript 参照实现的现场基准。

## 二、背景与动机

- 浏览器物理引擎要么是 JS（JIT 去优化悬崖、GC 停顿打穿 16.6ms 帧预算），要么是 C/Rust 编译的重型方案。MoonBit 生成高质量 wasm，但「无分配、硬实时」的模拟负载还没有 MoonBit 先例——我们想把这个空白填上。
- 物理是比滤镜更好的 wasm 试金石：分支密集、指针追逐、随机内存访问，且每帧预算刚性。它还能自然生长出好玩的演示——布料撕裂、金字塔爆破都是求解器的免费赠品。
- 技术上要回答三个问题：约束系统怎么在 wasm-gc 里做到零分配；场景状态怎么低成本过边界给 canvas 渲染；和 V8 的 JIT 比到底谁快——第三个我们决定当场实测而不是引用宣传数字。

## 三、功能亮点

- **四类物体一套求解器**：小球（圆-圆碰撞）、刚体箱（四角粒子 + 四边杆 + 两根隐藏对角撑）、吊绳（钉住端点的链）、布料（网格 + 可撕链接）。
- **全交互**：抓取工具拖动任意粒子并自然甩出（Verlet 隐式速度免记账）；球/箱/绳/布一键放置；💥 径向爆炸；重力/弹性/摩擦/求解精度四个滑杆全部实时生效。
- **四个场景预设**：300 球池、箱子金字塔、幕布 + 球雨（球把布撞破）、锁链与球。
- **撕布**：布料链接超过 2.4 倍静止长度即断裂——用手抓住一个节点狠狠一拽就能撕开幕布。
- **性能面板**：粒子数、fps 徽章；wasm vs JS 双规模基准（300 / 1500 粒子），同初态同算法。

## 四、技术架构与实现（MoonBit 技术要点）

1. **`src/lib`** — 纯 MoonBit 可移植求解器。Verlet 隐式速度让位置修正自动变成速度：反弹从「推回边界内」免费获得，抓取甩出不需显式速度记账。刚体=粒子簇；布料撕裂挂在约束求解里；空间哈希每步重建一次保证碰撞 O(n)；声部级零分配（全部预分配 FixedArray）。11 个单元测试。
2. **`src/kernel`** — wasm `foreign_library`，20 个导出：命令式（spawn_*/grab/drag/release/explode/step/set_*）+ 快照式（snapshot_out/in）。渲染快照每帧一次调用、一次 typed-array 读，零逐对象跨界。
3. **`web/`** — 画布渲染器（Float64Array 读位置 + Uint32Array 读类型，混视图零拷贝）、工具栏、场景预设、JS 参照求解器与基准、零依赖服务器。

**关键技术攻关**：

- **f64 精确快照**：粒子记录按 48 字节存放位置/半径/上一帧位置（全 f64），`snapshot_out → snapshot_in` 往返**字节级一致**——由「确定性测试」（同状态 + 同步数 ⇒ 快照逐字节相等）钉死。这也是基准公平性的根基：两个实现从同一组比特出发。
- **诚实的基准曲线**：debug 构建的 wasm 曾以 0.27× 惨败给 JS（moonpix 时代热循环是手写内联 WAT 不受构建模式影响，这次是 MoonBit 生成的代码——release 构建反超）。最终公开的数字：300 粒子 0.99×（平手）、1500 粒子 1.15×、2000 粒子 1.56×——规模越大 JIT 优势越被稀释，wasm 无去优化、无 GC 停顿的稳定性胜出。数字全部现场可复现。
- **混合视图渲染**：一个共享内存缓冲上叠 Float64Array（坐标）与 Uint32Array（粒子类型）两个视图，浏览器端零拷贝、零解析。
- 工程实录：`1e-6` 不是合法的 MoonBit 字面量；`fn meth(self : T)` 旧式方法语法已弃用需写 `fn T::meth`；`init` 是保留函数名；快照里速度存储改存上一帧位置后往返才达到字节精确——每一条都来自真实踩坑。

## 五、性能与验证

- **求解吞吐**：300 粒子 0.32 ms/步（帧预算 16.6 ms 的 2%）、1200 粒子 4.1 ms、2000 粒子 9.6 ms（57%）——全部远低于 60fps 红线；页面实测 240fps（240Hz 刷新率跑满）。
- **质量门槛全绿**：`moon check` 零警告；`moon test` 11/11（自由落体闭式解 `y₀+g·dt²·n(n+1)/2`、约束收敛、箱体翻滚中对角线保持 5% 内、回弹比、爆炸冲量 19.000 精确、布料撕裂、三箱堆叠 600 步不散架）；内核端到端 **10/10**（含字节级确定性、抓取甩出速度、快照往返、实时预算断言）。
- **真实浏览器端到端**：四场景渲染正常、拖拽甩飞、爆炸、撕布全部可用；基准面板双规模数字与 Node 一致。

## 六、运行与演示

```bash
moon build --target wasm --release
cp _build/wasm/release/build/src/kernel/kernel.wasm web/physics.wasm
node web/server.mjs 8095
# 打开 http://127.0.0.1:8095
```

30 秒演示脚本：点「箱子金字塔」→ 等它落稳 → 按 💥 看刚体箱四散翻滚 → 换「幕布 + 球雨」看布料被砸破 → 选「抓取」抓住任意东西甩出去 → 点「▶ 运行基准」看双规模竞速当场出数。

## 七、展望

- **铰链与滑轮**：约束系统再加两类关节，可搭机关/桥索；
- **流体倾向**：粒子数上探 1 万（SIMD 化空间哈希 + 双线程 step）；
- **碰撞形状**：边-点碰撞让箱体真正面面堆叠；
- **回放系统**：确定性已字节级成立，录下输入即可完整回放/分享关卡；
- **生态**：把 `src/lib` 发到 mooncakes.io，作为 MoonBit 的可移植 PBD 物理库。
